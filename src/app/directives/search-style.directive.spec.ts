import { SearchStyleDirective } from './search-style.directive';

describe('SearchStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchStyleDirective();
    expect(directive).toBeTruthy();
  });
});
